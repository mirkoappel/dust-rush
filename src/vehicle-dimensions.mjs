// Shared game metres. The original Blender chassis is retained at this scale;
// only its longitudinal spacing grows. Wheel size and track remain unchanged.
export const VEHICLE_DIMENSIONS=Object.freeze({
  modelScale:.62,
  sourceWheelbase:2.0832,
  chassisStretch:1.2,
  wheelbase:2.49984,
  track:2.2072,
  wheelRadius:.685,
});
